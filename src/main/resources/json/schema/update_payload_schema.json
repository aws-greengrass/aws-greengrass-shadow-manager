{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Update Shadow Payload Schema",
  "description": "Schema to validate the payload recieved for update shadow",
  "type": "object",
  "properties": {
    "clientToken": {
      "description": "The unique identifier for the client making the update shadow request",
      "type": "string",
      "maxLength": 64
    },
    "state": {
      "description": "The state node containing the shadow state.",
      "type": "object"
    },
    "version": {
      "description": "The version of the shadow update request.",
      "type": "number",
      "minimum": 0
    }
  },
  "required": [
    "state"
  ]
}